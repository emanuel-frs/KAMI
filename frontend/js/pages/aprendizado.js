/**
 * Página: aprendizado
 * TODO: portar o markup/lógica correspondente de kami_telas_final.html
 * e ligar aos endpoints de ../api/aprendizado.js. Essa tela NÃO usa o grid
 * de widgets configurável (isso é exclusivo de perfil/núcleo,
 * decisão 17) — layout fixo definido pelo módulo.
 */
export async function mount(container) {
  container.innerHTML = '<div class="wg-placeholder" style="padding:16px;">página "aprendizado" — a implementar</div>';
}

export function unmount() {}
